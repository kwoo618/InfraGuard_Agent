fastapi>=0.110,<0.116
# starlette는 fastapi 0.115.x의 요구 범위로 고정한다.
# 핀이 없으면 설치 시점에 비호환 최신(예: 1.3.1)이 딸려와
# APIRouter() 생성 시 TypeError(on_startup)로 서버가 기동되지 않는다. (#43)
starlette>=0.40.0,<0.47.0
uvicorn[standard]>=0.29
httpx>=0.27
pydantic>=2.6
python-dotenv>=1.0
sse-starlette>=2.0
docker>=7.0
langfuse>=2.0
pytest>=8.0
pytest-asyncio>=0.23
