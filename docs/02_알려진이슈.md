# 02. 알려진 이슈 (코드 기준, main @ c776348)

각 이슈는 "코드에서 확인된 사실"과 "실행으로 확인해야 할 것"을 구분한다.
실행 확인 결과는 이 문서에 기록한다.

> 2026-09-11 추가: ISSUE-2·3 보강, ISSUE-6~9 신규, ISSUE-1·2·6 실행 확인 결과. 코드 기준 HEAD e0c49db (c776348 이후 코드 변경 없음).
> 2026-09-11 Phase 2 (nginx 로드밸런서, HEAD 5a27416 + 작업 트리): ISSUE-1·9 적용 내용과 ISSUE-1·2·6·9 실행 확인 결과 추가. nginx 1.28.3, Locust 2.46.4.
> ISSUE-6~8의 라이브러리 기본값 근거는 prometheus-fastapi-instrumentator 소스(GitHub master, PyPI 8.1.0 wheel)이다.
> target-server `Dockerfile`은 이 라이브러리 버전을 고정하지 않는다. 실제 설치 버전은 ISSUE-6 확인란에 기록한다.

---

## ISSUE-1 [치명] 스케일 아웃해도 부하가 새 replica로 분산되지 않을 가능성

**코드에서 확인된 사실**
- `docker-compose.yml`: target-server가 `ports: "8080-8089:8080"`로 replica마다 호스트 포트를 하나씩 받는다.
  앞단에 로드밸런서(nginx 등)가 없다.
- `infra/locust/locust.conf`: `host = http://localhost:8080`
- `backend/app/tools/run_load_test.py`: `DEFAULT_HOST = os.getenv("TARGET_SERVER_URL", "http://localhost:8080")`
- 백엔드를 호스트에서 uvicorn으로 실행하면 부하는 `localhost:8080` = **replica 1개**로만 간다.
- 백엔드를 `--profile app` 컨테이너로 실행하면 `http://target-server:8080`(Docker DNS)을 쓰는데,
  분산은 클라이언트의 DNS 해석·keep-alive 동작에 좌우되어 균등하다는 보장이 없다.

**영향**
스케일링 전/후 비교가 "replica 증가 효과"를 측정하지 못한다.
전/후 수치 차이가 나와도 스케일링 효과인지 측정 노이즈인지 구분할 수 없다.

**실행으로 확인할 것** (ISSUE-2 해결 후 가능)
1. `docker compose up -d --scale target-server=3`
2. 평소 실행 방식 그대로 부하 테스트 실행
3. Prometheus에서 `sum by (instance) (rate(http_requests_total[1m]))` → replica별 요청이 고르게 들어오는가?

**해결 방향:** target-server 앞에 nginx 로드밸런서 서비스 추가, 부하는 nginx로 보낸다. (03_대시보드명세.md Phase 2)

- [x] 실행 확인 결과 (2026-09-11, Phase 1 적용 후):
  - 조건: replica 3개, Locust 50 users 60초, `--host http://localhost:8084` (replica 1개의 호스트 포트.
    재생성 후 8080이 비어 있어 이 포트를 썼다 → ISSUE-9)
  - `sum by (instance) (rate(http_requests_total[1m]))`: `172.18.0.3` 하나만 약 47 req/s, 나머지 2개는 0
  - Locust: 2829 req, 47.49 req/s, 실패 0.42% (`/flaky`만 5.41%), `/heavy` P95 3200ms, 평균 2557ms
  - 결론: replica를 늘려도 부하는 1개로만 간다. **이 수치는 발표용 before 값이 아니라 ISSUE-1 증거로만 쓴다.**

**Phase 2 적용** (코드): target-server 앞에 nginx 로드밸런서를 추가했다(`infra/nginx/nginx.conf`, `docker-compose.yml`).
부하는 `localhost:8080`(호스트) / `nginx:8080`(컨테이너 네트워크) → nginx → replica 라운드로빈으로 간다.
backend 컨테이너의 `TARGET_SERVER_URL`도 `http://nginx:8080`으로 바꿔 호스트 실행과 같은 경로로 측정한다.
Phase 2 이전 수치(replica 직결)는 nginx를 거치는 이후 수치와 비교 대상이 아니다.

- [x] Phase 2 적용 후 실행 확인 (2026-09-11):
  - 분산: replica 3개, Locust 50 users 60초(`locust.conf` host `localhost:8080` = nginx).
    `sum by (instance) (rate(http_requests_total[1m]))`(부하 종료 직전): `172.18.0.7` 32.48 / `172.18.0.3` 32.07 / `172.18.0.6` 32.31 req/s.
    3개 모두 요청을 받았고 거의 균등하다.
    Locust: 6447건, 108.20 req/s, 실패 0.47%(`/flaky` 500 30건뿐), `/heavy` 평균 476ms, P95 770ms.
  - 1→3 스케일 직후: 부하(50 users) 중 `scale_service(3)` 호출 → 7.6초 후 반환(`--wait` 포함, before 1 → after 3).
    `rate(...[15s])` 기준으로 새 replica는 반환 후 10~15초에 요청 수신이 관측됐고, 20초 후에는 3개가 약 38 req/s씩 균등했다.
    관측 지연에는 Prometheus dns_sd 갱신(5s) + scrape(5s) + rate 창이 포함된다.
  - 부하 중 scale-down(3→1) 순간: 100초 실행 8405건 중 `/flaky` 500 46건 외에 `/light` 504가 1건 있었다
    (약 2000ms, nginx 로그 `upstream timed out while connecting to upstream`). 제거되는 replica로 가던 요청이다.
    에이전트 흐름은 부하 테스트가 끝난 뒤 스케일링하므로 측정 중에는 scale-down이 일어나지 않는다.
    이 100초 실행의 `/heavy` P95(3100ms)는 replica 1개 구간이 섞인 값이라 비교용이 아니다.
  - 위 수치는 nginx 경유 조건의 검증 기록이며 발표용 before/after 값이 아니다.
  - UI 흐름 (DEBUG 꺼짐, replica 1, target_tps 50): 진단은 정상 완료됐다(TPS 60.6, P95 2800ms, 활성 연결 37).
    LLM은 스케일링 불필요로 판단했다. 진단 문구의 "CPU/메모리 0%로 자원 병목 없음"은 고정값(ISSUE-3)이
    LLM 입력에 들어간 결과다 → 후속 이슈에서 처리한다(아직 미등록).
  - 스케일링 경로 기계 검증 (`DEBUG_ENDPOINTS_ENABLED=true` + `force_scaling`, 검증 후 즉시 끔):
    승인 모달 → 1→2 스케일 → 재측정 → 리포트까지 정상 동작했다. 승인부터 재측정 완료까지 약 34초(재측정 부하 10초 포함).
    강제 실행이므로 이 실행의 측정값은 기록하지 않는다.

---

## ISSUE-2 [높음] Prometheus가 replica 1개만 수집 — Phase 1 해결

**코드에서 확인된 사실**
- `infra/prometheus/prometheus.yml`: `static_configs: targets: ["target-server:8080"]` 단일 타깃.
  파일 주석에도 스케일 후 과소집계 문제와 `dns_sd_configs` 해결책이 적혀 있다.
- `backend/app/tools/get_metrics.py`: `sum(http_requests_in_progress) or vector(0)` → 1개 replica 값만 합산됨.

**영향:** 스케일 후 Grafana TPS/P95/connection 수치가 실제보다 작거나 왜곡된다.
(connection 수는 ISSUE-6 때문에 수정 전에는 항상 0이었다.)

**해결 방향:** `dns_sd_configs`(names: target-server, type: A, port: 8080)로 교체.

**Phase 1 적용:** `prometheus.yml`의 target-server job을 `dns_sd_configs`
(names: `target-server`, type: `A`, port: `8080`, refresh_interval: `5s`)로 교체했다.
`get_metrics.py` 쿼리는 바꾸지 않았다. `sum(...)`이 디스커버리된 타깃 전체를 합산한다.

- [x] 실행 확인 결과 (2026-09-11, replica 3):
  - `promtool check config`: SUCCESS
  - `/targets`: target-server 3/3 UP (instance `172.18.0.3` / `172.18.0.6` / `172.18.0.7` `:8080`)
  - `count(up{job="target-server"} == 1)` = 3
  - [x] scale 1로 줄인 뒤 결과 (2026-09-11 Phase 2 검증): 부하 중(Locust 50 users, nginx 경유) `scale_service(1)` 호출 → 4.0초 후 반환
    (`before_replicas=3, after_replicas=1, success=True`). 반환 5초 뒤 조회에서는 아직 3, 10초 뒤 `count(up{job="target-server"} == 1)` = 1.
    `sum by (instance) (rate(http_requests_total[15s]))`도 instance 1개(`172.18.0.3`)만 남았다.

---

## ISSUE-3 [중간] Windows Docker Desktop에서 cAdvisor 패널이 비어 있음

**코드에서 확인된 사실**
- `infra/grafana/dashboard.json`의 CPU/메모리/Replica 패널이 `name=~"infraguard-target-server.*"` 라벨을 사용.
  "CPU 사용률 추이" 패널도 같은 라벨을 사용한다.
- 일일보고 Day3: Windows에서 cAdvisor `name` 라벨 미지원 확인 → `get_metrics.py`의 cpu_pct/mem_pct는 0.0 고정.
  (일일보고는 repo 밖 자료다. repo 안의 근거는 `get_metrics.py` docstring과 `cpu_pct=0.0`, `mem_pct=0.0` 고정 코드다.)

**해결 방향**
- Replica 수: `count(up{job="target-server"} == 1)`로 대체 (ISSUE-2 해결 후 동작).
- CPU/메모리: 시연 PC OS에서 실제로 값이 나오는지 확인 후 유지/제거 결정. 값이 안 나오는 패널은 발표 화면에 두지 않는다.

- [ ] 시연 PC OS: ___ / CPU·메모리 패널 동작 여부: ___
- 2026-09-11 Phase 2 UI 검증에서 영향 확인: LLM 진단 문구에 "CPU/메모리 0%로 자원 병목 없음"이 나왔다.
  `get_metrics.py`의 `cpu_pct=0.0`, `mem_pct=0.0` 고정값이 진단 입력(`SystemMetrics`)에 실측처럼 들어가 판단 근거로 쓰였다. → 후속 이슈에서 처리(아직 미등록).

---

## ISSUE-4 [중간] Grafana 프로비저닝 없음

**코드에서 확인된 사실**
- `docker-compose.yml`: `./infra/grafana`를 `/var/lib/grafana/dashboards`에 마운트만 한다.
  datasource / dashboard provider 프로비저닝 파일이 없다.
- `dashboard.json`의 모든 패널은 datasource `uid: "prometheus"`를 참조한다.

**영향:** 새 환경에서 `docker compose up` 후 데이터소스와 대시보드를 수동으로 만들어야 한다. 시연 중 실수 위험.

**해결 방향:** `infra/grafana/provisioning/{datasources,dashboards}/` 추가, datasource uid를 `prometheus`로 고정.

---

## ISSUE-5 [낮음] 측정 이력이 메모리에만 있고 1회분만 보관

**코드에서 확인된 사실**
- `backend/app/api/v1/agent.py`: `before_measurements[task_id]`에 스케일링 전 값을 **최초 1회만** 저장.
  이후 `state["load_test_result"]`는 재검증 값으로 덮어써진다.
- 다회차 스케일링 시 중간 라운드 측정값은 사라진다. 서버 재시작 시 전부 사라진다.
- UI(`static/main.js`)는 `/report`의 전/후 값을 **로그 텍스트로만** 출력한다.

**영향:** 발표용 실측 증빙을 남길 수 없고, 라운드별 추이를 시각화할 수 없다.

**해결 방향:** 라운드별 측정 이력 누적 + JSON 파일 저장 + UI 비교 차트. (03_대시보드명세.md Phase 3~4)

---

## ISSUE-6 [높음] in-progress 게이지가 노출되지 않아 connection_count가 항상 0 — Phase 1 해결

**코드에서 확인된 사실**
- `infra/target-server/main.py`: `Instrumentator().instrument(app).expose(app)` — 옵션 없이 기본값 사용.
- 라이브러리 기본값: `should_instrument_requests_inprogress=False`, `inprogress_name="http_requests_inprogress"`.
  켜더라도 기본 이름이 쿼리의 `http_requests_in_progress`와 다르다.
- `backend/app/tools/get_metrics.py`: `sum(http_requests_in_progress) or vector(0)` → 메트릭이 없으므로 `or vector(0)`로 항상 0.
- `infra/grafana/dashboard.json` "활성 Connection 수" 패널도 같은 쿼리를 쓴다.
- `connection_count`는 LLM 진단 입력(`SystemMetrics`)과 `backend/app/tools/generate_plan.py`의 `connection_count >= 100` 분기에서 사용된다.

**수정 전 런타임 확인** (예전 이미지, 재빌드 전)
- `/metrics`에 `inprogress` / `in_progress` 이름의 메트릭이 없음.

**영향:** 에이전트는 connection 수를 항상 0으로 받은 채 진단한다. Grafana 패널도 항상 0이다.

**해결 방향:** target-server에서 `should_instrument_requests_inprogress=True`, `inprogress_name="http_requests_in_progress"`로 켠다.
쿼리는 그대로 두고 메트릭 이름을 쿼리에 맞춘다. (Phase 1)
수정 후에는 `connection_count`가 실측값이 되므로 LLM 진단 입력이 달라진다.

- [x] 재빌드 후 확인 (2026-09-11):
  - 설치된 instrumentator 버전: `prometheus-fastapi-instrumentator` 8.1.0
  - Prometheus에 `http_requests_in_progress` 시리즈가 instance 3개 모두 존재
- [x] 부하 중 `sum(http_requests_in_progress)`가 0이 아닌 값 (2026-09-11 Phase 2 검증):
  - 조건: replica 3개, Locust 50 users 60초, nginx 경유(`localhost:8080`)
  - 부하 구간 5초 간격 8회 조회: 21 / 23 / 16 / 19 / 15 / 20 / 17 / 21 — 모두 0이 아니다.

---

## ISSUE-7 [중간] Grafana 에러율 패널의 라벨 불일치

**코드에서 확인된 사실**
- `infra/grafana/dashboard.json` "에러율 — 엔드포인트별" 패널:
  `sum by (handler) (rate(http_requests_total{status_code=~"5.."}[1m])) / sum by (handler) (rate(http_requests_total[1m]))`
- 라이브러리 `metrics.default()`의 `http_requests_total` 라벨은 `method`, `status`, `handler`이다.
  기본값 `should_group_status_codes=True`라 `status` 값이 `"2xx"`, `"5xx"`처럼 묶인다.

**수정 전 런타임 확인** (예전 이미지, 재빌드 전)
- `http_requests_total{handler="/light",method="GET",status="2xx"}` 형태. `status_code` 라벨 없음.

**영향:** 분자 쿼리가 항상 빈 결과라 에러율 패널이 비어 있다.
에이전트가 쓰는 `error_rate`는 Locust 결과(`LoadTestResult`)라서 이 문제의 영향을 받지 않는다.

**해결 방향:** Phase 5에서 라벨을 `status="5xx"`로 고친다. 이번 Phase에서는 코드를 수정하지 않는다.

---

## ISSUE-8 [중간] Grafana P95 패널이 1000ms에서 상한

**코드에서 확인된 사실**
- `infra/grafana/dashboard.json` "P95 응답 시간 (ms)" 패널:
  `histogram_quantile(0.95, sum by (handler, le) (rate(http_request_duration_seconds_bucket[1m]))) * 1000`
- 라이브러리 `metrics.default()`의 `http_request_duration_seconds` 히스토그램은 `latency_lowr_buckets=(0.1, 0.5, 1)`을 쓴다.
- Prometheus 공식 문서(`histogram_quantile`): quantile이 최상위 버킷(+Inf)에 있으면 두 번째로 높은 버킷의 상한을 반환하고,
  버킷 안에서는 선형보간한다.

**수정 전 런타임 확인** (예전 이미지, 재빌드 전)
- `http_request_duration_seconds_bucket`의 `le` = `0.1`, `0.5`, `1.0`, `+Inf`

**영향:** 실제 P95가 1s를 넘어도 패널에는 1000ms로 표시된다. 0.1~1s 구간도 버킷 3개 사이 선형보간이라 정밀도가 낮다.
- 참고: ISSUE-1 실행에서 Locust가 잰 `/heavy` P95는 3200ms였다. 이 구간에서 Grafana P95 패널은 1000ms로 잘려 보일 것으로 예상된다(패널 표시는 미확인).

**해결 방향:** Phase 5에서 처리한다. 이번 Phase에서는 코드를 수정하지 않는다.
발표용 P95는 Locust 측정값(`LoadTestResult.latency_p95`)을 쓴다.

---

## ISSUE-9 [높음] 컨테이너 재생성 후 호스트 포트 8080이 비어 부하 대상이 사라짐

**코드에서 확인된 사실**
- `docker-compose.yml`: target-server가 `ports: "8080-8089:8080"` **범위 매핑**이라, replica가 어느 호스트 포트를 받을지 고정되지 않는다.
- 부하 대상은 `localhost:8080` 고정이다: `infra/locust/locust.conf`의 `host = http://localhost:8080`,
  `.env.example`의 `TARGET_SERVER_URL=http://localhost:8080`, `run_load_test.py`의 `DEFAULT_HOST` 기본값.

**실행 확인** (2026-09-11)
- `--build --scale target-server=3`으로 재생성한 뒤 replica들이 호스트 포트 8084 / 8085 / 8086을 받았고, 8080은 비어 있었다.
- `localhost:8080` 대상 Locust 60초: 637건 전부 `ConnectionRefusedError` (WinError 10061). 응답시간 약 4000ms, 에러율 100%로 집계됐다.

**영향**
- 스케일링으로 컨테이너가 재생성되면 부하 대상(`TARGET_SERVER_URL`, `locust.conf` host)이 사라질 수 있다.
- 연결 실패가 "높은 latency + 에러율 100%" 측정값으로 에이전트에 전달되어 병목으로 오진될 수 있다.
- `tests/integration/test_e2e.py::test_agent_real_e2e_full_verification` 실패(2026-09-11, 에이전트 status=failed)와 관련됐을 가능성이 있다(미확인).
  `pytest tests/unit`은 95 passed. e2e는 Phase 2 이후 다시 확인한다.

**해결 방향:** Phase 2에서 해결한다. 호스트 포트가 고정된 nginx 로드밸런서를 두고 부하 대상을 nginx로 바꾼다(03_대시보드명세.md Phase 2).
이번 Phase에서는 코드를 수정하지 않는다.

**Phase 2 적용** (코드): target-server의 호스트 포트 매핑을 삭제하고 nginx가 호스트 8080을 고정으로 받는다.
target-server를 재생성해도 부하 대상 주소가 유지된다. 부하 대상 기본값 3곳은 바꾸지 않았다(이제 nginx를 가리킨다).

- [x] Phase 2 적용 후 실행 확인 (2026-09-11):
  - `docker compose up -d --wait --force-recreate --scale target-server=3 target-server`로 replica 3개를 재생성했다.
    replica 포트는 `8080/tcp`(호스트 매핑 없음), nginx가 `0.0.0.0:8080`을 받는다.
  - 재생성 직후 `curl http://localhost:8080/health` → 200
  - 이어서 Locust 50 users 15초: 1423건, `ConnectionRefusedError` 0건. 실패 11건(0.77%)은 전부 `/flaky` 500이다.
  - 참고: 재생성 중(replica가 0개인 순간) nginx 로그에 `target-server could not be resolved (110: Operation timed out)`가 1회 남았고 이후 자동 복구됐다. 부하가 없던 구간이다.
  - e2e 재확인 (2026-09-11): `pytest tests/` 97 passed(e2e 포함). 단, e2e는 LLM이 "병목 없음"으로 판단해
    스케일링 없이 종료된 경로(비스케일 경로)로 통과했다. 스케일링 경로는 ISSUE-1의 UI 검증(force_scaling)에서 확인했다.
